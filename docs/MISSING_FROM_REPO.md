# Elementos que faltaban en repo

Antes de esta estabilizaci?n faltaban o estaban incompletos:

- `app/front_controller.php` para enrutar legacy PHP a URLs vivas.
- `app/snapshots/` con produccion viva como fuente de verdad visual/textual.
- `app/translations/no.php` y `no` en `brand.langs`.
- `public/assets/img/flags/no.svg`.
- Hero y assets criticos descargados de produccion.
- `public/contact-submit.php`.
- Sitemap limpio sin `/` y con `/no/`.
- `.htaccess` coherente para `/` -> `/es/`, idiomas y rutas anidadas.
- Documentaci?n de despliegue dev y rollback.

No se han eliminado `/no/`, landings de zona ni reformas.
